# 比赛提交检查清单

## 代码与模型

- [x] pytest、Ruff、mypy 全部通过。
- [x] 模型 manifest SHA-256 与模型卡一致。
- [x] 部署包恰好包含 5 个模型成员。
- [x] Docker 构建上下文不依赖本机外部目录。
- [x] 镜像以非 root 用户运行，模型和演示病例只读。

## Nexent

- [ ] 容器端口为 8000，MCP 路径为 `/mcp`。
- [x] 本地容器的 `/health/live` 和 `/health/ready` 返回 200。
- [ ] Nexent 只显示 6 个 `crc_lnm_*` 工具。
- [ ] Qwen3-32B 系统提示词已粘贴并保存。
- [ ] 每病例同 trace 的 QC→双特征→预测→报告链路通过。

## 医学口径

- [x] 全部材料统一为 CT 1409 维、病理 768 维、临床 4 项。
- [x] 未声称处理原始 DICOM、ROI、WSI、patch 或生成热图。
- [x] OOF ROC-AUC 只作为开发参考。
- [x] 明确 `independent_test_claim=false`。
- [x] 报告包含人工复核和科研辅助安全声明。

## 数据与安全

- [x] 演示数据只提供 `demo_case_001` 合成病例。
- [x] 不含患者身份、真实标签、训练数据或原始影像。
- [x] 不含平台密码、API key、token、私钥或本机绝对路径。
- [x] Bearer token 配置为通过平台 Secret 注入。
- [ ] 若使用无 Bearer 内网配置，已确认服务没有公网可达路径。

## 提交材料

- [ ] Demo 运行视频。
- [x] 代码、提示词、模拟数据、测评数据集和测试结果。
- [ ] 完善后的方案设计书 Word。
- [ ] 智能体功能介绍 Word，包含平台访问方式。
- [ ] 截图和视频已遮挡账号、内部地址和凭据。
