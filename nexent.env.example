# Copy this file outside source control or export these values from the platform.
# Never commit the real bearer token.
NEXENT_DOCKER_NETWORK=nexent_network
CRC_LNM_MCP_BEARER_TOKEN=
CRC_LNM_MCP_ALLOWED_ORIGINS=http://nexent-web:3000,http://nexent:3000

