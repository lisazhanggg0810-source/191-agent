# 使用说明

## 1. 本项目做什么

本项目将已冻结的五成员结直肠癌淋巴结转移模型封装为 MCP 服务。它只读取服务端白名单病例包中的预提取数据，按“质控 -> 双模态特征准备 -> 预测 -> 报告”的固定顺序返回结构化科研辅助结果。

输入规模固定为 CT 1409 维、病理 768 维和 4 项临床变量。模型不接收患者身份信息、原始影像、切片或客户端给出的文件路径。

## 2. 截图问题与修复

服务创建页提示“服务配置 JSON 结构不符合要求，请以 `mcpServers` 作为顶层字段”。旧 README 在环境变量章节给出了以下类型的 JSON：

```json
{
  "CRC_LNM_MCP_BEARER_TOKEN": {
    "required": true
  }
}
```

这只是环境变量说明，不是 MCP 服务配置。导入器读取 README 后把它作为服务配置，因此无法识别服务器。

修复后的可导入文件是 [configs/modelscope-mcp.json](configs/modelscope-mcp.json)。它的根字段为 `mcpServers`，并以 `uv` 在仓库根目录启动本项目的 stdio MCP：

```json
{
  "mcpServers": {
    "crc-lnm-research-assistant": {
      "command": "uv",
      "args": [
        "run",
        "--extra",
        "mcp",
        "crc-lnm-mcp",
        "--config",
        "configs/mcp.local.yaml",
        "--transport",
        "stdio"
      ]
    }
  }
}
```

## 3. GitHub 快速创建步骤

1. 将整个 `competition_release_1` 目录提交到一个 GitHub 仓库，仓库根目录必须保留 `pyproject.toml`、`configs/`、`src/`、`models/`、`data/`、`scripts/` 和 `demo/`。
2. 在平台选择“从 GitHub 仓库快速创建”，填写仓库地址、英文名称和中文名称。
3. 在“服务配置”中粘贴 `configs/modelscope-mcp.json` 的完整 JSON。不要粘贴 README 里的环境变量表。
4. 提交创建，等待平台安装依赖并启动 stdio 服务。
5. 创建完成后检查是否识别出且只识别出 6 个以 `crc_lnm_` 开头的工具。
6. 在智能体中启用这 6 个工具，并把 [docs/AGENT_PROMPT.md](docs/AGENT_PROMPT.md) 的全文设置为系统提示词。

该路径不需要 URL、端口和 Bearer token，因为服务进程由托管平台直接启动。服务配置中的 `configs/mcp.local.yaml` 是有意指定的：安装后的 Python 包不携带仓库外的 YAML，显式传入仓库配置可保证模型、病例和 artifact 路径正确解析。

## 4. 工具调用顺序

1. 每次会话先调用 `crc_lnm_get_model_info`，确认模型完整性、5 个成员和输入维度。
2. 每个病例生成新的 UUIDv4 `trace_id`。
3. 调用 `crc_lnm_case_data_qc`。若失败，不得继续。
4. 使用同一 `trace_id` 和 QC artifact 调用 CT、病理特征准备工具。
5. 两项特征准备均成功后，使用两个 artifact 和四项临床变量调用预测工具。
6. 预测成功后调用报告工具。

所有数值、类别和模型版本必须直接来自工具返回值。`demo_case_001` 仅用于工程演示和回归检查，不能作为临床性能证据。

## 5. 本地开发与自检

前提：Python 3.12 或 3.13，已安装 `uv`。

```powershell
uv sync --extra mcp --extra validation
uv run --extra mcp --extra validation pytest -q
uv run --extra mcp crc-lnm-mcp --config configs/mcp.local.yaml --transport stdio
```

本地启动时使用 `mcp.local.yaml`，仅绑定本机并关闭 Bearer 校验。运行后产生的 `artifacts_local/` 是临时 artifact 存储，删除服务测试结果前可直接删除该目录；不要将其提交到 GitHub。

## 6. Docker / HTTP 部署

若目标平台要求 HTTP 服务而非 stdio，则使用 Dockerfile：

```powershell
docker build -t crc-lnm-mcp:competition .
docker run --rm -p 8000:8000 `
  -e CRC_LNM_MCP_BEARER_TOKEN="<平台 Secret>" `
  -e CRC_LNM_MCP_ALLOWED_HOSTS="<平台服务名>:*,localhost:*,127.0.0.1:*" `
  -e CRC_LNM_MCP_ALLOWED_ORIGINS="http://nexent:3000" `
  crc-lnm-mcp:competition
```

检查 `http://127.0.0.1:8000/health/live` 和 `http://127.0.0.1:8000/health/ready`。MCP 地址为 `http://<服务名>:8000/mcp`，并需携带 `Authorization: Bearer <平台 Secret>`。不要在 Git 仓库、README、截图或智能体提示词中写入真实 Secret。

## 7. 提交前检查

- `configs/modelscope-mcp.json` 可以被 JSON 解析，且根字段仅为 `mcpServers`。
- `pytest -q` 通过，且不生成需要提交的缓存或 artifact。
- GitHub 仓库不含 `artifacts_local/`、Python 缓存、真实病例、token、密钥或机器绝对路径。
- 平台页面只显示 6 个 `crc_lnm_*` 工具。
- 智能体输出明确为科研辅助，不使用确诊、排除、保证准确等表述。
