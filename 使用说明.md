# 使用说明

## 1. 项目用途

本项目是结直肠癌淋巴结转移多模态科研辅助 MCP 服务。它加载一个已经验证完整性的五成员模型，对受控病例包执行固定流程：病例质控、CT 特征校验、病理特征校验、多模态推理和 HTML 报告生成。

模型固定使用 1409 维 CT 特征、768 维病理特征以及 `age`、`male`、`Type`、`T`。服务不接收原始 DICOM、NIfTI、WSI、切片编号、patch、CSV/PT 路径或任意上传文件。

## 2. 截图问题的原因与处理

截图中的 `PA1104647output.nii.gz` 是原始 CT 影像，`PA1104647_6` 是 WSI 切片标识。它们不是本模型的输入，因而不能直接提交给推理 Agent。截图所要求的 1218 维 CT CSV 与 1024 维 patch PT 也与本发布包的 1409+768 模型权重不兼容。

正确链路如下：

```text
受信任 JSONL 病例记录
  -> 生成受控病例包并重新计算文件哈希
  -> case_ref
  -> QC -> CT 特征校验 + 病理特征校验
  -> 五成员多模态推理 -> 科研辅助报告
```

若只有原始影像，请先使用与当前模型训练特征完全同源的上游提取流程生成 1409 维 CT 和 768 维病理特征，并按本项目的 JSONL 契约发布。不要把不同维度的特征补零、截断、重排或交给大模型转换。

## 3. JSONL 与 iData

`data/release_case_package_groups.jsonl` 含 142 条已脱敏病例记录。每条记录包含 `case_ref`、manifest、临床字段、CT 特征和病理特征。依据 iData 26.3.0 用户指南，独立部署的数据集输出格式为 `jsonl`；指南还说明单次本地上传默认最多 2000 个文件、总大小最高 5 GB、单文件最高 500 MB。本数据集约 12 MB、142 行，处于这些默认限制内。

服务运行时不直接把 JSONL 暴露为 MCP 入参。`configs/mcp.local.yaml` 会在首次启动时将它原子转换到 `artifacts_local/cases/`，随后通过源文件 SHA-256 检查复用缓存。缓存与 JSONL 不匹配时会拒绝启动，避免混用不同版本的病例包。

## 4. 本地运行与测试

准备 Python 3.12 或 3.13 与 `uv`，在项目根目录执行：

```powershell
uv sync --extra mcp --extra validation
uv run --extra mcp --extra validation pytest -q -p no:cacheprovider
uv run --extra mcp crc-lnm-mcp --config configs/mcp.local.yaml --transport stdio
```

首次 stdio 启动会校验并生成 142 个本地病例包。启动后可使用 `PA1104647` 进行演示。测试覆盖：全量 JSONL 的特征名称和维度、病例包哈希、`PA1104647` 的完整推理流程、模型完整性、HTTP 鉴权和六个 MCP 工具的精确注册。

若更新了 JSONL，先删除本地生成的 `artifacts_local/cases/`，再启动服务以重建缓存。不要删除 `data/release_case_package_groups.jsonl`、`models/` 或 `demo/`。

## 5. 托管 MCP 配置

在 ModelScope MCP 广场选择“从 GitHub 仓库快速创建”时，推荐填写 README 完整地址 `https://github.com/lisazhanggg0810-source/191-agent/blob/main/README.md`，不要使用带 `/tree/main` 的分支页面地址。平台会从 `README.md` 的 `json` 代码块解析配置；该代码块必须与 `configs/modelscope-mcp.json` 完全一致，根对象为 `mcpServers`，并通过 `uvx --no-progress --torch-backend cpu --from crc-lnm-medical-agent==1.0.6 crc-lnm-medical-agent` 显式启动 PyPI 1.0.6。该命令不使用 `@latest`，也不要求在平台另行填写 `UV_TORCH_BACKEND`。不要加入仓库相对路径、URL、Docker 命令、Bearer token 或本地文件路径。

发布包已经包含不可变的模型资源和病例 JSONL，控制台入口默认使用 stdio；首次启动会在平台可写的运行时目录自动建立缓存。

若页面超过 60 秒仍停留在“解析中”，取消并刷新创建页后使用上述 README 完整地址重试；仍未完成时，点击“进入自定义创建”，选择 STDIO，并粘贴 `configs/modelscope-mcp.json` 的完整内容，以绕过 GitHub 自动解析。

创建智能体后，添加六个 `crc_lnm_*` 工具，并把 `docs/AGENT_PROMPT.md` 完整内容设置为系统提示词。

## 6. Docker / HTTP 部署

Docker 构建会在镜像构建期转换 JSONL，不在容器启动后修改 `/app`：

```powershell
docker build -t crc-lnm-mcp:competition .
docker run --rm -p 8000:8000 `
  -e CRC_LNM_MCP_BEARER_TOKEN="<至少32字节随机值>" `
  -e CRC_LNM_MCP_ALLOWED_HOSTS="<平台服务名>:*,localhost:*,127.0.0.1:*" `
  -e CRC_LNM_MCP_ALLOWED_ORIGINS="http://nexent:3000" `
  crc-lnm-mcp:competition
```

健康检查为 `GET /health/live` 和 `GET /health/ready`，MCP 地址为 `http://<平台服务名>:8000/mcp`。HTTP 模式必须通过平台 Secret 注入 `Authorization: Bearer <token>`；只有平台无法注入 Header 且服务确实完全隔离时，才可使用 `configs/mcp.nexent-internal.yaml`。

## 7. 边界

输出仅用于科研辅助和病例复核，必须由具备资质的人员复核。不要在日志、截图、报告或提示词中泄露直接身份标识、原始影像数据、特征向量、模型权重、token、API key 或服务器绝对路径。
